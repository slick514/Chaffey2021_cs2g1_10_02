//
// Created by Justin Gries on 11/1/2021.
//

#include "clock.h"

/**
 * TODO:
    To get the current time, use the following code, which requires that you include the <ctime> header:
    time_t current_time = time(0);
    tm* local_time = localtime(&current_time);
    int hours = local_time->tm_hour;
    int minutes = local_time->tm_min;
 */
int Clock::get_hours(){
    return 0;
}

/**
 * TODO:
    To get the current time, use the following code, which requires that you include the <ctime> header:
    time_t current_time = time(0);
    tm* local_time = localtime(&current_time);
    int hours = local_time->tm_hour;
    int minutes = local_time->tm_min;
 */
int Clock::get_minutes(){
    return 0;
}

/**
 * TODO call the get_hours and get_minutes functions to generate this string
 */
std::string Clock::get_time() {
    return std::string();
}
